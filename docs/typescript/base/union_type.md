# union types

```ts
// this variable is going to hold a string or a number
// and either one is okay.
let something: string | number;

// It's not limited to only two data types when you created a union type.
let isActive: number | boolean | string;
```