# 加入配置对象

```js
let data = Object.assign({
            inputButton: this,
            fileSize: data.fileSize || "1024MB",
            sessionid: data.sessionid,
            // item.__uploadOptions : 追加的配置对象
          }, item && item.__uploadOptions ? item.__uploadOptions : {});
```



