# Git Clone - Provide A Password With The Remote URL

```shell
git clone https://username:password@git.test.com/Demo/demo-1.git
```

This is not safe as the password will be in the shell (ex. bash) history.